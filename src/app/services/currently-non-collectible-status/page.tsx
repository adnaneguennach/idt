import { Metadata } from "next";
import ClientPage from "./Client";

export const metadata: Metadata = {
  title: "Currently Non Collectible Status - Ideal Tax",
  description: "Learn more about Currently Non Collectible Status with Ideal Tax's expert services.",
};

export default function Page() {
  return <ClientPage />;
}
